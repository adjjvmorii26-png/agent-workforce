name: organism-sync
description:

tags: [synchronize, organism, simulate, html, visualize, map]
steps:
  - name: Model
    prompt: "Describe 4 'organisms' (subsystems) and how they synchronize around: {input}"
  - name: Visual
    prompt: "Produce a self-contained HTML page (inline CSS/JS, no CDNs) visualizing the synchronization from: {prev}"
